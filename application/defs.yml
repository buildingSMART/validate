Validation using JsonSchema for checking the config
---
tags:
  - config
parameters:
  - name: body
    in: formData
    required: true
    schema:
      id: User
      required:
        - syntax
        - schema
        - mvd
        - bsdd
      properties:
        syntax:
          type: string
          default: "0"
        schema:
          type: string
          default: "0"
        mvd:
          type: string
          default: "0"
        bsdd:
          type: string
          default: "0"
