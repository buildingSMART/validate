from django.apps import AppConfig


class IfcValidationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.ifc_validation'
    verbose_name = 'IFC VALIDATION'  # name in Django Admin
